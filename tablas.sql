CREATE TABLE PRODUCTOS(id int not null primary key auto_increment,name varchar(50) not null,stock int not null);INSERT INTO PRODUCTOSI(name, total) VALUES ('tunas', 500);CREATE TABLE VENTAS(	id int not null primary key auto_increment,	total_ventas int);CREATE TABLE DESCRIPCION_VENTA(	id int not null auto_increment primary key,	id_producto int not null,	id_venta int not null);ALTER TABLE DESCRIPCION_VENTA	ADD CONSTRAINT fk_producto		FOREIGN KEY(id_producto)		  REFERENCES PRODUCTOS(id)		    ON DELETE SET NULL;ALTER TABLE DESCRIPCION_VENTA	ADD CONSTRAINT fk_venta		FOREIGN KEY(id_venta)		  REFERENCES VENTAS(id)			ON DELETE SET NULL;	CREATE PROCEDURE IDENTIFY_VALUE(num_one, num_two, num_tree)	BEGIN		DECLARE mayor int;		DECLARE medio int;		IF num_one > num_two AND num_one > num_tree THEN			SET mayor := num_one;			  IF num_two > num_tree THEN					SET medio := num_two;			  	ELSE 					SET medio := num_tree;			  END IF;		ELSE IF num_two > num_one AND num_two > num_tree THEN 			SET mayor := num_two;			 	IF num_one > num_tree THEN					SET medio := num_one;				  ELSE					SET medio := num_tree;				END IF;		ELSE IF num_tree > num_one AND num_tree > num_two THEN			SET mayor := num_tree;			 	IF num_one > num_two THEN					SET medio := num_one;				  ELSE					SET medio := num_two;				END IF;	  		END IF;			END;